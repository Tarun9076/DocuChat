# Core app (app/), eval harness (eval/), and the numbered learning scripts (learning/*.py).
# Pinned to what's verified working together; bump deliberately, not with a blanket upgrade.
Flask==3.1.3
python-dotenv==1.2.2
langchain-core==1.4.9
langchain-openai==1.3.5
langchain-chroma==1.1.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-classic==1.0.8
chromadb==1.5.9
sentence-transformers==5.6.0
pypdf==6.14.2
rank-bm25==0.2.2

# Production WSGI servers (pick the one for your OS — see wsgi.py)
gunicorn==26.2.0
waitress==3.0.2
