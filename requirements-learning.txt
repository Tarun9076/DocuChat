# Extra dependencies only needed by specific files under learning/ — install
# on top of requirements.txt if you want to run these:
#   5_recursive_character_text_spliiter.py  -> langchain
#   6_semantic_chunking.py                  -> langchain-experimental
#   12_hybrid_search.ipynb, 13_reranker.ipynb -> langchain-cohere
#   8_multi_modal_rag.ipynb                 -> unstructured[all-docs], pdf2image
# Not required for the webapp, eval/, or the rest of learning/ — kept separate
# so the main install stays fast and small.
langchain==1.3.14
langchain-experimental
langchain-cohere==0.6.0
unstructured[all-docs]==0.18.32
pdf2image==1.17.0
